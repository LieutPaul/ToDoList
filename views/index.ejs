<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles.css">
    <title>To Do List</title>
</head>
<body>
    <div class="box" id="heading">
        <h1 style="color:purple;"><%= day %></h1>
    </div>
    
    <div class="box">
        <% for(var i=0;i<listItems.length;i++){ %>
            <form action="/delete" method="post">
                <div class="item">
                    <input type="checkbox" name="id" value="<%= listItems[i]._id %>" onChange="this.form.submit()">
                    <p><%= listItems[i].name %></p>
                </div>
                <input type="hidden" name="listName" value="<%= day%>">
            </form>
        <%}%>
        <form class="item" action="/" method="post">
            <input type="text" name="newItem" placeholder="Add New Item" autocomplete="off">
            <button type="submit" name="add" value = "<%=day%>">+</button>
        </form>
    </div>
</body>
</html>